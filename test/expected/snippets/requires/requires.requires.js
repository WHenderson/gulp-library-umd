// cjs
// requires
require("lib-a"), require("lib-b"), require("lib-c"), require("lib-d")
// requiresVars
var libA = require("lib-a"), libB = require("lib-b"), libC = require("lib-c"), libD = require("lib-d");

// node
// requires
require("lib-a"), require("lib-b"), require("lib-c"), require("lib-d")
// requiresVars
var libA = require("lib-a"), libB = require("lib-b"), libC = require("lib-c"), libD = require("lib-d");

// amd
// requires
require("lib-a"), require("lib-b"), require("lib-c-amd")
// requiresVars
var libA = require("lib-a"), libB = require("lib-b"), libC = require("lib-c-amd");

// web
// requires
require("lib-a"), require("lib-b"), require("lib-c"), require("lib-d")
// requiresVars
var libA = require("lib-a"), libB = require("lib-b"), libC = require("lib-c"), libD = require("lib-d");

