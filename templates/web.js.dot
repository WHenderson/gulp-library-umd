{{ mode = 'web'; }};(function ({{= it.factory.args }}) {
  {{= it.contents }}
  root.{{= it.options.namespace }} = {{#def.exports}};
}).call(this{{?it.mode[mode].args.length != 0}},{{?}}{{#def.globalsMapped}});