{{ mode = 'web'; }};(function ({{= it.factory.args }}) {
  {{= it.contents }}
  root.{{= it.options.namespace }} = {{#def.exportsJs}};
}).call(this{{?it.mode[mode].args.length != 0}},{{?}}{{#def.globalsMappedJs}});